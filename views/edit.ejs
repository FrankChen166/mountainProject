<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Product</title>
  </head>
  <body>
    <h1>Edit Product</h1>
    <form
      action="/product/<%= product._id %>/detail/detailInfo?detailId=<%= detail._id %>"
      method="post"
    >
      <div>
        <label for="name">Product Name:</label>
        <input
          type="text"
          name="name"
          id="name"
          required
          value=" <%= detail.name %>"
        />
      </div>
      <div>
        <label for="quantity">Product Quantity:</label>
        <input
          type="number"
          name="quantity"
          id="quantity"
          required
          value="<%= detail.quantity%>"
        />
      </div>
      <div>
        <label for="color">Product Color:</label>
        <input
          type="text"
          name="color"
          id="color"
          required
          value="<%= detail.color %>"
        />
      </div>
      <button type="submit">Update</button>
    </form>
    <a href="/product/<%= product._id%>">回到細項</a>
  </body>
</html>
